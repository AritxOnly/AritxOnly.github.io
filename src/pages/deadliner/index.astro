---
export const prerender = true;

const APP = {
  name: "Deadliner",
  tagline: "更快把事情做完的任务与习惯系统",
  sub: "本地优先 · WebDAV 同步 · AI 解析 · 极简但强大",
  primaryCta: { text: "下载 Android 版", href: "#download" },
  secondaryCta: { text: "查看 GitHub", href: "https://github.com/AritxOnly/Deadliner" },
  links: {
    privacy: "/privacy",
    changelog: "/changelog",
    share: "/deadliner/share",
  },
};
---

<!doctype html>
<html lang="zh-CN">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>{APP.name} · {APP.tagline}</title>
    <meta name="description" content={`${APP.tagline} — ${APP.sub}`} />

    <!-- Open Graph (可选，建议后面把图片补上) -->
    <meta property="og:title" content={`${APP.name} · ${APP.tagline}`} />
    <meta property="og:description" content={APP.sub} />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="/deadliner" />

    <style>
      :root{
        --bg: #0b0c0f;
        --panel: rgba(255,255,255,.06);
        --panel2: rgba(255,255,255,.08);
        --text: rgba(255,255,255,.92);
        --muted: rgba(255,255,255,.62);
        --line: rgba(255,255,255,.12);
        --shadow: 0 24px 60px rgba(0,0,0,.45);
        --radius: 22px;
        --max: 1120px;
        --accent: #4da3ff;
      }
      *{box-sizing:border-box}
      html,body{height:100%}
      body{
        margin:0;
        font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, "PingFang SC","Hiragino Sans GB","Microsoft YaHei", Arial, "Noto Sans", "Apple Color Emoji","Segoe UI Emoji";
        color:var(--text);
        background:
          radial-gradient(1200px 800px at 20% -10%, rgba(77,163,255,.25), transparent 55%),
          radial-gradient(900px 600px at 90% 10%, rgba(153,102,255,.18), transparent 50%),
          radial-gradient(900px 700px at 40% 110%, rgba(0,255,187,.10), transparent 55%),
          var(--bg);
        overflow-x:hidden;
      }
      a{color:inherit; text-decoration:none}
      .wrap{max-width:var(--max); margin:0 auto; padding:0 20px}
      .nav{
        position:sticky; top:0; z-index:50;
        backdrop-filter: blur(14px);
        background: rgba(11,12,15,.6);
        border-bottom:1px solid var(--line);
      }
      .nav-inner{display:flex; align-items:center; justify-content:space-between; height:64px}
      .brand{display:flex; align-items:center; gap:10px; font-weight:650; letter-spacing:.2px}
      .dot{
        width:10px; height:10px; border-radius:99px;
        background: linear-gradient(135deg, var(--accent), #9b7bff);
        box-shadow: 0 0 0 4px rgba(77,163,255,.12);
      }
      .nav-links{display:flex; gap:18px; align-items:center; color:var(--muted); font-size:14px}
      .nav-links a:hover{color:var(--text)}
      .btn{
        display:inline-flex; align-items:center; justify-content:center;
        height:40px; padding:0 14px;
        border-radius:999px;
        border:1px solid var(--line);
        background: rgba(255,255,255,.04);
        color:var(--text);
        font-size:14px;
        transition: transform .18s ease, background .18s ease, border-color .18s ease;
        gap:8px;
      }
      .btn:hover{transform: translateY(-1px); background: rgba(255,255,255,.07); border-color: rgba(255,255,255,.18)}
      .btn.primary{
        border-color: rgba(77,163,255,.35);
        background: rgba(77,163,255,.18);
      }
      .btn.primary:hover{background: rgba(77,163,255,.26)}
      .hero{
        padding:72px 0 34px;
      }
      .hero-grid{
        display:grid; gap:28px;
        grid-template-columns: 1.15fr .85fr;
        align-items:center;
      }
      @media (max-width: 920px){
        .hero-grid{grid-template-columns:1fr}
      }
      .kicker{
        display:inline-flex; align-items:center; gap:10px;
        padding:8px 12px; border-radius:999px;
        border:1px solid var(--line);
        background: rgba(255,255,255,.04);
        color:var(--muted); font-size:13px;
      }
      .title{
        margin:14px 0 10px;
        font-size:56px;
        line-height:1.02;
        letter-spacing:-.8px;
      }
      @media (max-width: 520px){ .title{font-size:42px} }
      .subtitle{
        margin:0 0 18px;
        color:var(--muted);
        font-size:16px;
        line-height:1.6;
        max-width:58ch;
      }
      .cta{display:flex; flex-wrap:wrap; gap:12px; margin-top:18px}
      .meta{
        margin-top:18px; display:flex; flex-wrap:wrap; gap:14px; color:var(--muted); font-size:13px;
      }
      .meta span{display:inline-flex; align-items:center; gap:8px}
      .pill{
        width:7px; height:7px; border-radius:99px;
        background: rgba(255,255,255,.25);
      }
      .device{
        border-radius: var(--radius);
        border: 1px solid var(--line);
        background: linear-gradient(180deg, rgba(255,255,255,.08), rgba(255,255,255,.03));
        box-shadow: var(--shadow);
        padding:18px;
      }
      .device-top{
        display:flex; justify-content:space-between; align-items:center;
        padding:10px 10px 14px;
        border-bottom:1px solid rgba(255,255,255,.08);
        color: var(--muted);
        font-size:13px;
      }
      .screen{
        margin-top:16px;
        border-radius: 18px;
        border: 1px solid rgba(255,255,255,.12);
        background:
          radial-gradient(600px 400px at 30% 20%, rgba(77,163,255,.18), transparent 60%),
          radial-gradient(500px 380px at 80% 30%, rgba(155,123,255,.14), transparent 55%),
          rgba(0,0,0,.35);
        height: 360px;
        display:flex; align-items:center; justify-content:center;
        color: rgba(255,255,255,.6);
        font-size:14px;
      }
      .section{padding:34px 0}
      .section h2{
        margin:0 0 10px;
        font-size:28px; letter-spacing:-.2px;
      }
      .section p.lead{
        margin:0 0 22px;
        color:var(--muted);
        line-height:1.7;
        max-width:70ch;
      }
      .grid3{
        display:grid; gap:14px;
        grid-template-columns: repeat(3, 1fr);
      }
      @media (max-width: 920px){ .grid3{grid-template-columns:1fr} }
      .card{
        border-radius: var(--radius);
        border: 1px solid var(--line);
        background: rgba(255,255,255,.04);
        padding:18px;
      }
      .card h3{margin:0 0 8px; font-size:16px}
      .card p{margin:0; color:var(--muted); line-height:1.7; font-size:14px}
      .split{
        display:grid; gap:14px;
        grid-template-columns: 1fr 1fr;
      }
      @media (max-width: 920px){ .split{grid-template-columns:1fr} }
      .faq details{
        border-radius: 16px;
        border: 1px solid var(--line);
        background: rgba(255,255,255,.03);
        padding:14px 16px;
      }
      .faq details + details{margin-top:10px}
      summary{cursor:pointer; list-style:none; font-weight:600}
      summary::-webkit-details-marker{display:none}
      .faq p{color:var(--muted); margin:10px 0 0; line-height:1.7; font-size:14px}

      footer{
        padding:34px 0 50px;
        color:var(--muted);
        border-top:1px solid var(--line);
        margin-top:24px;
      }
      .foot{
        display:flex; gap:16px; justify-content:space-between; align-items:flex-start; flex-wrap:wrap;
      }
      .foot a{color:var(--muted)}
      .foot a:hover{color:var(--text)}

      /* smooth scroll */
      html{scroll-behavior:smooth}

      /* subtle appear */
      .fade{
        animation: fadeUp .55s ease both;
      }
      @keyframes fadeUp{
        from{opacity:0; transform: translateY(10px)}
        to{opacity:1; transform: translateY(0)}
      }
    </style>
  </head>

  <body>
    <header class="nav">
      <div class="wrap nav-inner">
        <div class="brand">
          <span class="dot"></span>
          <span>{APP.name}</span>
        </div>
        <nav class="nav-links">
          <a href="#features">特性</a>
          <a href="#security">隐私</a>
          <a href="#download">下载</a>
          <a class="btn" href={APP.links.share}>分享链接</a>
        </nav>
      </div>
    </header>

    <main>
      <section class="hero">
        <div class="wrap hero-grid">
          <div class="fade">
            <div class="kicker">
              <span class="pill"></span>
              <span>独立落地页 · 不依赖全站布局</span>
            </div>
            <h1 class="title">{APP.tagline}</h1>
            <p class="subtitle">{APP.sub}</p>

            <div class="cta">
              <a class="btn primary" href={APP.primaryCta.href}>{APP.primaryCta.text}</a>
              <a class="btn" href={APP.secondaryCta.href} target="_blank" rel="noreferrer">GitHub</a>
              <a class="btn" href={APP.links.privacy}>隐私政策</a>
            </div>

            <div class="meta">
              <span><span class="pill"></span> 本地优先</span>
              <span><span class="pill"></span> WebDAV 同步</span>
              <span><span class="pill"></span> AI 显式触发</span>
              <span><span class="pill"></span> Android</span>
            </div>
          </div>

          <div class="device fade" style="animation-delay:.08s">
            <div class="device-top">
              <span>Deadliner · Preview</span>
              <span>v4.x</span>
            </div>
            <div class="screen">
              这里放截图：public/deadliner/hero.webp
            </div>
          </div>
        </div>
      </section>

      <section id="features" class="section">
        <div class="wrap">
          <h2>为“把事做完”设计</h2>
          <p class="lead">
            目标不是堆功能，而是让你在更少的操作里完成更多的计划执行：输入更快、回顾更清晰、同步更可靠。
          </p>

          <div class="grid3">
            <div class="card">
              <h3>本地优先</h3>
              <p>离线可用，数据在你设备上。同步只是可选增强，不是依赖。</p>
            </div>
            <div class="card">
              <h3>WebDAV 同步</h3>
              <p>用你自己的云盘（坚果云等）同步快照；不需要注册账号，不绑定平台。</p>
            </div>
            <div class="card">
              <h3>AI 显式触发</h3>
              <p>你点一下才调用。Key 由你自己提供，App 只做解析与填空。</p>
            </div>
            <div class="card">
              <h3>Classic / Simplified</h3>
              <p>同一套数据，两种交互密度，按你当下的状态切换。</p>
            </div>
            <div class="card">
              <h3>通知与日历</h3>
              <p>提醒只做提醒；日历读写用于导入/生成事件，逻辑可控。</p>
            </div>
            <div class="card">
              <h3>可导出/可删除</h3>
              <p>你始终可以导出你的数据；需要时也能彻底清空。</p>
            </div>
          </div>
        </div>
      </section>

      <section id="security" class="section">
        <div class="wrap split">
          <div class="card">
            <h3>隐私与合规取向</h3>
            <p>
              不强制登录；不收集邮箱/手机号；不做行为分析；同步与 AI 走你自备服务。
              这让它更接近“工具软件”而不是“平台”。
            </p>
          </div>
          <div class="card">
            <h3>你已经有 /deadliner/share</h3>
            <p>
              这个落地页不会干扰分享处理页面。路由上是 /deadliner 与 /deadliner/share 分离。
            </p>
          </div>
        </div>
      </section>

      <section id="download" class="section">
        <div class="wrap">
          <h2>获取 Deadliner</h2>
          <p class="lead">把下载入口、版本渠道、二维码都集中在这里。你后面把链接替换成真实地址即可。</p>

          <div class="grid3">
            <div class="card">
              <h3>Android APK</h3>
              <p>放你的直链 / Release 链接。</p>
              <div style="margin-top:14px">
                <a class="btn primary" href="#" onclick="return false">下载 APK</a>
              </div>
            </div>
            <div class="card">
              <h3>GitHub Releases</h3>
              <p>适合开发者与追新用户。</p>
              <div style="margin-top:14px">
                <a class="btn" href={APP.secondaryCta.href} target="_blank" rel="noreferrer">打开 GitHub</a>
              </div>
            </div>
            <div class="card">
              <h3>更新日志</h3>
              <p>跳到站内 changelog 或外链。</p>
              <div style="margin-top:14px">
                <a class="btn" href={APP.links.changelog}>查看变更</a>
              </div>
            </div>
          </div>
        </div>
      </section>

      <section class="section">
        <div class="wrap">
          <h2>常见问题</h2>
          <div class="faq" style="margin-top:14px">
            <details>
              <summary>为什么不用账号体系？</summary>
              <p>因为这是工具：账号会引入绑定、迁移成本、合规与服务器长期运维。你用 WebDAV 自持更可控。</p>
            </details>
            <details>
              <summary>AI 会不会自动上传我的内容？</summary>
              <p>不会。你显式触发才会请求；Key 在你手里；App 只是 prompt/解析载体。</p>
            </details>
            <details>
              <summary>后面要挂到 VPS 域名吗？</summary>
              <p>可以。你现在先用 IP 跑通，之后把域名 A 记录指向 VPS，并上 HTTPS 即可。</p>
            </details>
          </div>
        </div>
      </section>

      <footer>
        <div class="wrap foot">
          <div>
            <div style="display:flex; align-items:center; gap:10px; font-weight:650; color:var(--text)">
              <span class="dot"></span><span>{APP.name}</span>
            </div>
            <div style="margin-top:10px">© {new Date().getFullYear()} {APP.name}. All rights reserved.</div>
          </div>

          <div style="display:flex; gap:14px; flex-wrap:wrap">
            <a href="/deadliner">/deadliner</a>
            <a href={APP.links.share}>/deadliner/share</a>
            <a href={APP.links.privacy}>隐私政策</a>
            <a href={APP.secondaryCta.href} target="_blank" rel="noreferrer">GitHub</a>
          </div>
        </div>
      </footer>
    </main>
  </body>
</html>